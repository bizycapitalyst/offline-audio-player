# TTS engine — used by prerender.py / prerender_gui.py / prerender_web.py
edge-tts>=6.1.10

# Multi-format text extraction (only loaded when the corresponding format is used)
python-docx>=1.1.0    # .docx
pypdf>=4.0.0          # .pdf
ebooklib>=0.18        # .epub
beautifulsoup4>=4.12  # .epub HTML cleanup

# English -> Latin American Spanish translation (used when --lang is spanish or both)
deep-translator>=1.11.0

# Drag-and-drop in the Tkinter GUI (optional — file picker works without it)
tkinterdnd2>=0.3.0; sys_platform != "darwin"

