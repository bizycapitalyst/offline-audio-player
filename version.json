{
  "version": "1.3.1",
  "released": "2026-05-09",
  "notes": "Fix playback stalling a few seconds in: the v1.3.0 seek-on-loadedmetadata for position resume was racing with audio.play() and aborting it on mobile browsers. Drop the speculative position-resume scope (folder + track persistence — what was actually requested — still work). Tracks now always start from 0; folder and last-played track still restore."
}
