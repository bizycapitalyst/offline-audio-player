{
  "version": "1.3.0",
  "released": "2026-05-09",
  "notes": "Session persistence: folder selection (FileSystemDirectoryHandle in IndexedDB), last-played track baseName, audio playback position, and TTS sentence index are saved across closes. On reopen, if the browser still grants permission to the saved folder it loads silently; otherwise a 'Resume previous folder' button appears that re-requests permission with a single tap."
}
