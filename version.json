{
  "version": "1.8.9",
  "released": "2026-05-07",
  "notes": "Paragraph glyph (¶) on the skip-back / skip-forward buttons is now visually scaled to 1.7× via transform, so it reads clearly inside the chip while the button itself stays exactly the same size (transforms don't affect layout)."
}
